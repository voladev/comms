{
  "name": "comms",
  "version": "1.0.0",
  "description": "Gas Town communications rig — platform-agnostic messaging layer",
  "type": "module",
  "bin": {
    "comms": "./bin/comms"
  },
  "scripts": {
    "start": "tsx src/cli/serve.ts",
    "test": "vitest run",
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
